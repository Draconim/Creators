@extends("home")

@section("content")
<div class="container">
    <div class="card">
        <div class="card-header">{{ $events->name}}</div>
        <div class="card-body"></div>
    </div>
</div>
@endsection